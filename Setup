#!/usr/bin/setup
# -*- mode: shell-script -*-
Setup.use Pkg
declare DIST=.dist
case "$1" in
    ''|package)
	Pkg.package Pkg.files $(for file in $(find bin lib -type f); do echo "usr/$file=$file"; done)
	;;
esac
